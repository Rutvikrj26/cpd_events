# Django Settings
DJANGO_SETTINGS_MODULE=config.settings.development
DJANGO_SECRET_KEY=change-this-to-a-random-secret-key
DEBUG=True

# Database Configuration
DB_NAME=cpd_events
DB_USER=cpd_user
DB_PASSWORD=cpd_password
DB_HOST=localhost
DB_PORT=5432

# Redis
REDIS_URL=redis://localhost:6379/0

# Site Configuration
SITE_URL=http://localhost:8000
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Email Configuration (Mailgun for Production)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=info@accredit.store
SERVER_EMAIL=server@accredit.store

# Mailgun (Production only)
MAILGUN_SMTP_LOGIN=
MAILGUN_SMTP_PASSWORD=
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_SMTP_SERVER=smtp.mailgun.org
MAILGUN_SMTP_PORT=587

# GCP Configuration
GCP_PROJECT_ID=dev-project
GCP_LOCATION=us-central1
GCP_QUEUE_NAME=default
GCP_SA_EMAIL=service-account@example.com
GCS_BUCKET_NAME=

# Stripe Payment Configuration
STRIPE_SECRET_KEY=sk_test_
STRIPE_PUBLISHABLE_KEY=pk_test_
STRIPE_WEBHOOK_SECRET=whsec_

# ============================================================================
# IMPORTANT: Stripe pricing is now managed via Django Admin!
# ============================================================================
# You no longer need to manually set price IDs here.
# Instead:
#   1. Go to Django Admin → Billing → Stripe Products
#   2. Create/edit products and prices
#   3. Django will auto-sync to Stripe
#
# The variables below are kept for backward compatibility only.
# If database has no products, system will fall back to these values.
# ============================================================================

# Stripe Price IDs - Individual Plans (Monthly) [LEGACY - Use Django Admin instead]
STRIPE_PRICE_STARTER=price_
STRIPE_PRICE_PROFESSIONAL=price_
STRIPE_PRICE_PREMIUM=price_

# Stripe Price IDs - Individual Plans (Annual - 17% discount) [LEGACY - Use Django Admin instead]
STRIPE_PRICE_STARTER_ANNUAL=price_
STRIPE_PRICE_PROFESSIONAL_ANNUAL=price_
STRIPE_PRICE_PREMIUM_ANNUAL=price_

# Stripe Price IDs - Organization Plans [LEGACY - Use Django Admin instead]
STRIPE_PRICE_TEAM=price_
STRIPE_PRICE_TEAM_ANNUAL=price_
STRIPE_PRICE_ENTERPRISE=price_

# Billing Configuration
# NOTE: Trial days are now configured per-product in Django Admin (StripeProduct.trial_period_days)
# This global value is used as fallback only
BILLING_TRIAL_DAYS=14
BILLING_GRACE_PERIOD_DAYS=30
BILLING_DEFAULT_PLAN=attendee

# Zoom Integration
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
ZOOM_REDIRECT_URI=http://localhost:8000/api/integrations/zoom/callback/
ZOOM_WEBHOOK_SECRET=

# Security & Encryption
ENCRYPTION_KEY=YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXoxMjM0NTY=

# Admin Configuration
ADMIN_EMAIL=info@accredit.store
