port: 7880
rtc:
  port_range_start: 7882
  port_range_end: 7892
  use_external_ip: false
  tcp_port: 7881

keys:
  devkey: secret_dev_key_change_in_production

# Required by livekit-egress (and any other psrpc consumer).
redis:
  address: redis:6379

logging:
  level: info

webhook:
  urls:
    - http://backend:8000/api/v1/webhooks/video/
  api_key: devkey
