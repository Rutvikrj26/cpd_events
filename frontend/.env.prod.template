# =============================================================================
# FRONTEND PRODUCTION - Build-time variables
# =============================================================================
# VITE_API_URL is auto-set from Terraform output during `accredit cloud frontend deploy`.
# Only add variables here that aren't managed by Terraform.
# =============================================================================

# Google Maps API Key (get from console.cloud.google.com, restrict to your domains)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Stripe Publishable Key (get live key from dashboard.stripe.com/apikeys)
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_xxx
